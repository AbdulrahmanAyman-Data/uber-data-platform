<!--
    NOTE (from adaptation review): this file is currently NOT referenced by
    Dockerfile.master's COPY line (it only copies core-site.xml, hdfs-site.xml,
    mapred-site.xml, yarn-site.xml — not this one), and Dockerfile.slave just
    inherits the master image, so it never gets a separate slave-specific
    yarn-site.xml either. It looks like a leftover from an earlier design
    where slaves had their own yarn-site.xml. Left here (hostname corrected,
    in case you want to wire it in later), but it currently has zero effect
    on the running cluster — every container uses the yarn-site.xml baked
    into the (shared) master image.
-->
<configuration>
    <!-- ResourceManager hostname -->
    <property>
        <name>yarn.resourcemanager.hostname</name>
        <value>uber-hadoop-master</value>
    </property>

    <!-- Services required for running MapReduce jobs -->
    <property>
        <name>yarn.nodemanager.aux-services</name>
        <value>mapreduce_shuffle</value>
    </property>

    <!-- Enable shuffle service for MapReduce -->
    <property>
        <name>yarn.nodemanager.aux-services.mapreduce.shuffle.class</name>
        <value>org.apache.hadoop.mapred.ShuffleHandler</value>
    </property>

    <!-- Memory allocated for NodeManager -->
    <property>
        <name>yarn.nodemanager.resource.memory-mb</name>
        <value>8192</value>
    </property>

    <!-- CPU cores allocated for NodeManager -->
    <property>
        <name>yarn.nodemanager.resource.cpu-vcores</name>
        <value>8</value>
    </property>

    <!-- Where logs are stored -->
    <property>
        <name>yarn.nodemanager.log-dirs</name>
        <value>/var/log/hadoop-yarn</value>
    </property>

    <!-- Container logs -->
    <property>
        <name>yarn.nodemanager.remote-app-log-dir</name>
        <value>/var/log/hadoop-yarn/apps</value>
    </property>
</configuration>
